[MSTP]
spanning-tree mode mst
spanning-tree mst config
     name 이름
     revision 번호
     instance 1 vlan #,#,#
     instance 2 vlan #,#,#

802.1 W      RSTP
802.1 S       MSTP

루트 스위치 선정(기본 32768)
spanning-tree vlan 번호 priority 값
spanning-tree vlan 번호 root primary
: 24576
spanning-tree vlan 번호 root secondary
: 28672

spanning-tree mst # root {primary | secondary}
spanning-tree mst # priority 값

spanning-tree mode mstp
spanning-tree mstp configuration
  name CCNP
  revision 1
  instance 1 vlan 10,20,30,40,50

[STP 조정기술]
- PortFast
- UplinkFast
- BackboneFast

STP 조정기술
1) PortFast: Forwarding Delay 생략(30초 절약)
     SW(config)# spanning-tree portfast default

    SW(config-if)# spanning-tree portfast

     라우터와 연결되는 트렁크포트
     SW(config-if)# spaninng-tree portfast trunk

     [PortFast + Access Port 설정]
     SW(config-if)# switchport host

2) UplinkFast
     Root SW를 제외한 스위치에서 설정(Priority값을 높힌다:49152)
     SW(config)# spanning-tree uplinkfast

3) BackboneFast
     SW(config)# spanning-tree backbonefast

[Timer 조정]
R(config)# spanning-tree vlan X hello-time 초
R(config)# spanning-tree vlan X max-age 초
R(config)# spanning-tree vlan X forward-time 초